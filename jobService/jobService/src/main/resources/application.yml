spring:
  application:
    name :jobService
  datasource:
    url: jdbc:postgresql://localhost:5432/job
    username: uthej
    password: 123456789
  jpa:
    database: POSTGRESQL
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 8081